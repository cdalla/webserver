#include "webserver.hpp"

int main()
{
	Webserver main("file");

	main.run();
	
    
    
    
    // std::vector<Server>::iterator it = servers.begin();
	// for (; it != servers.end(); ++it)
	// {
	// 	close((*it).socket);
	// }
	
	exit(0);
}